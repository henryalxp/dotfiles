# Dotfiles
Project for storing MD commands for cursor, instructions for Copilot or other reusable MD or dotfile

Link your commands to Windows using
```
mklink /D <link> <target>
```

Link your commands to linux using
```
ln -s <link> ~/.cursor/commands
```
